---
kind: "cortex-memory-candidate"
schema_version: "3"
plugin_version: "1.5.5"
session_id: "session_865cd5d64bbf"
thread_id: "thread_707907ca0577"
created_at: "2026-07-12T17:53:35.916Z"
device_id: "GANG1606-04cf7da9"
source: "cortex-chat-local-fallback"
---

# memory-candidate

## preferences
- 上次会话中所有共享记忆（preferences / projects / people / decisions / recent）都是空的，每次请求都要做大量空查询和重试，这是最可能的超时根因。你可以先在 DevTools 里验证这个假设——如果看到大量对 `_cortex/memory/*.md` 的空内容读取请求，那就确认了。

## projects
- 上次会话中所有共享记忆（preferences / projects / people / decisions / recent）都是空的，每次请求都要做大量空查询和重试，这是最可能的超时根因。你可以先在 DevTools 里验证这个假设——如果看到大量对 `_cortex/memory/*.md` 的空内容读取请求，那就确认了。

## people
- 上次会话中所有共享记忆（preferences / projects / people / decisions / recent）都是空的，每次请求都要做大量空查询和重试，这是最可能的超时根因。你可以先在 DevTools 里验证这个假设——如果看到大量对 `_cortex/memory/*.md` 的空内容读取请求，那就确认了。

## decisions
- 上次会话中所有共享记忆（preferences / projects / people / decisions / recent）都是空的，每次请求都要做大量空查询和重试，这是最可能的超时根因。你可以先在 DevTools 里验证这个假设——如果看到大量对 `_cortex/memory/*.md` 的空内容读取请求，那就确认了。

## recent
- 打开 Obsidian 的开发者工具（Help → Toggle Developer Tools），看 Console 里超时错误的具体来源是 cortex 插件还是网络请求。 我无法直接打开你本地 Obsidian 的开发者工具——我没有图形界面访问权限。但你可以自己操作，我来告诉你看什么、怎么判断来源。 **操作步
